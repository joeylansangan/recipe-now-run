# Copy to .env.local and fill in. Both stay server-side — never sent to the browser.

# https://console.anthropic.com  → API keys
ANTHROPIC_API_KEY=

# https://console.cloud.google.com → enable "YouTube Data API v3" → Credentials → API key
YOUTUBE_API_KEY=
